Create a MATLAB prototype scaffold for the paper-line project.

Requirements:
- deterministic seed
- separate modules for trajectory generation, prediction, follow-point scoring, state machine, logging, and plotting
- simple function interfaces
- easy to convert into a Live Script later
- minimal dependencies and R2024b-friendly code

Return only the scaffold plan and file layout unless I ask for code.
